Place these files here:

  headshot.jpg     — Professional headshot photo (used in About section)
  logo-dark.png    — ShliachFlow logo on transparent background, dark version
                     (for light/scrolled nav background)
  logo-white.png   — ShliachFlow logo on transparent background, white version
                     (for dark/transparent nav — optional, SVG wordmark is used as fallback)

Currently the site uses an SVG wordmark fallback for the nav logo.
To switch to the PNG logo, remove the <!-- REPLACE WITH LOGO PNG --> block
in index.html and uncomment the <img> tag beneath it.
